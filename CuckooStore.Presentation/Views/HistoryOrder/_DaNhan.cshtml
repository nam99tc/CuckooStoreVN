@model IEnumerable<CuckooStore.Models.Order>
@if (Model.Count() > 0)
{
    foreach (var item in Model)
    {
        Html.RenderAction("HistoryDetail", "HistoryOrder", new { id = item.OrderID });
    }
}
